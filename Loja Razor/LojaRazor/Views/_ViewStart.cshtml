@{ 
    <!--Toda vez que o razor monta a página de resposta para uma requisição, antes de ler qualquer view, ele processa o _ViewStart.cshtml, então se definirmos a variável Layout dentro desse arquivo, o mesmo Layout será utilizado para todas as views da aplicação.-->
    Layout = "~/Views/Shared/_Layout.cshtml";
}